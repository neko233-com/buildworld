// Distributed Worker support stays in the codebase for future explicit opt-in,
// but BuildWorld's current product surface and generated pipelines use builtin.
export const DISTRIBUTED_WORKERS_ENABLED: boolean = false
